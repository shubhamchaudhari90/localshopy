@* @model localshopyNew.Models.Shop

<h2>@Model.Name (@(Model.IsOpen ? "Open" : "Closed"))</h2>

<p><b>Phone No:</b> @Model.PhoneNo</p>
<p><b>Location:</b> @string.Join(" | ", Model.ServedLocations)</p>
<p><b>Owner Email:</b> @Model.OwnerEmailId</p>

<h3>Products</h3>
@if (Model.Products.Count == 0)
{
    <p>No products yet.</p>
}
else
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Discount</th>
                <th>Final Price</th>
                <th>Available</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var product in Model.Products)
            {
                <tr>
                    <td>@product.Name</td>
                    <td>@product.Description</td>
                    <td>@product.Price</td>
                    <td>@product.Discount %</td>
                    <td>@product.FinalPrice</td>
                    <td>@(product.IsAvailable ? "Yes" : "No")</td>
                </tr>
            }
        </tbody>
    </table>
}
 *@